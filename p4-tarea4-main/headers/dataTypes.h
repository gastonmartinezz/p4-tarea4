#include "../cpp/datatypes.cpp"

struct DTFecha {};

struct DTDireccion{};

struct DTPromocion{};

struct DTProducto{};

struct DTProducto{};

struct DTVendedor{};

struct DTNotificacion{};

struct DataPromocion{};

enum EnumCategoria{};